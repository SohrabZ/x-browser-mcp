# x-browser-mcp

@AGENTS.md

The brief lives in `AGENTS.md` and is imported above rather than copied here.
Two copies of the same document drift, and the interesting half of that document
is a list of things that are not obvious from the code — exactly the half that
goes stale silently. This project has already fixed the same defect in three
copies of one decision and shipped a leak in the copy that disagreed.
